{
  "name": "obsidian-emacs-isearch-mark",
  "version": "1.0.0",
  "description": "Emacs keybindings for Obsidian with Isearch and mark set",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --external:obsidian --external:@codemirror/* --external:@lezer/* --outfile=main.js --format=cjs",
    "dev": "esbuild src/main.ts --bundle --platform=node --external:obsidian --external:@codemirror/* --external:@lezer/* --outfile=main.js --format=cjs --watch"
  },
  "keywords": ["obsidian", "obsidian-plugin", "emacs", "keybindings"],
  "author": "myaccount-hash",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/myaccount-hash/obsidian-emacs-isearch-mark"
  },
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.27.2",
    "obsidian": "^1.11.0",
    "typescript": "^5.9.3"
  }
}
